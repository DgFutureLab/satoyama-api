box: wercker/python
services: 
	- wercker/postgresql
build:
	steps:
		- virtualenv:
	        name: setup virtual environment
	        install_wheel: false # Enable wheel to speed up builds (experimental)
		- pip-install
		- script:
			name: webapp tests
			code: py.test -v